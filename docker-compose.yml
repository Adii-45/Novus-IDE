services:
  mongo:
    image: mongo:7
    container_name: novuside-mongo
    restart: unless-stopped
    ports:
      - "27018:27017"
    volumes:
      - novuside-mongo-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  novuside-mongo-data:
